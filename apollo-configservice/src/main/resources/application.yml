spring:
  application:
    name: apollo-configservice
  profiles:
    active: ${apollo_profile}
  datasource:
    url : jdbc:mysql://127.0.0.1:3306/ApolloConfigDb
    username : root
    password : root
    driver-class-name : com.mysql.jdbc.Driver

ctrip:
  appid: 100003171
  
server:
  port: 8080

logging:
  file: /opt/logs/100003171/apollo-configservice.log
